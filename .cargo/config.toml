# RandomX (and optional RocksDB) need the C++ standard library. rust-lld alone
# cannot resolve `-lstdc++` in this environment; use g++ as the linker on Linux.
[target.x86_64-unknown-linux-gnu]
linker = "g++"
